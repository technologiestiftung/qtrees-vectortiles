services:
  - type: web
    name: qtrees-vectortiles
    env: docker
    plan: starter
    region: frankfurt
    autoDeploy: false
    dockerContext: .
    dockerfilePath: Dockerfile.render
    envVars:
      - key: POSTGRES_HOST
        sync: false
      - key: POSTGRES_DB
        sync: false
      - key: POSTGRES_USER
        sync: false
      - key: POSTGRES_PASSWORD
        sync: false
